rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /orders/{orderId} {
      // Allow customers to submit orders and dispatch admin to read and update order status
      allow read, create, update: if true;
    }
  }
}
